/*Arquivo contendo a função main do programa. O fluxo de execução principal do programa
 * deve ser controlado por meio dessa função.*/

#include "modulo_arquivos.h"
#include "modulo_interface.h"
#include "modulo_usuarios.h"

int main(){

	pedido *p_pedido;
	usuario *plista_usuarios;
	item *p_item;
	itens_pedido *p_itens_pedido;

	p_pedido = malloc(sizeof(pedido));
	p_usuario = malloc(sizeof(usuario));
	p_item = malloc(sizeof(item));
	p_itens_pedido = malloc(sizeof(itens_pedido));

	return 0;
}
